import Header from "@/components/Header";
import React from "react";

const page = () => {
  return (
    <main>
      <section>
        <Header />
      </section>
    </main>
  );
};

export default page;
